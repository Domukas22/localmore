import * as React from "react";

const Base = () => (
  <div>
    Welcome to the Admin Panel!
    {/* Add any other widgets or information you want to display on the dashboard */}
  </div>
);

export default Base;
